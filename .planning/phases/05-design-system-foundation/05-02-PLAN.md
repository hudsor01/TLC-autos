---
phase: 05-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - DSYS-05

must_haves:
  truths:
    - "Admin sidebar renders dark navy (#1e3a5f primary) against light content area"
    - "Admin Recharts charts are readable on light card backgrounds"
    - "Admin tables display with readable text contrast on warm white surfaces"
    - "Admin forms have visible input borders and focus rings"
    - "The full site renders correctly on OS dark mode — no dark backgrounds appear anywhere"
  artifacts: []
  key_links:
    - from: "src/app/globals.css"
      to: "src/app/admin/layout.tsx"
      via: "CSS variables --primary, --sidebar, --muted consumed by admin layout"
      pattern: "bg-primary|bg-sidebar"
---

<objective>
Verify the admin dashboard remains fully functional after dark mode removal and palette evolution. Confirm no visual regressions across the entire site.

Purpose: DSYS-05 requires admin dashboard verification. This is a critical safety check — the admin is used daily by 1-2 staff and must remain usable.
Output: Visual confirmation that admin and public pages render correctly with the new light-only palette.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-system-foundation/05-CONTEXT.md
@.planning/phases/05-design-system-foundation/05-01-SUMMARY.md

<interfaces>
<!-- Admin layout uses these CSS variables (must remain valid after palette evolution):
  - bg-primary → --primary: #1e3a5f (sidebar background, unchanged)
  - text-primary-foreground → --primary-foreground: #ffffff (sidebar text, unchanged)
  - bg-muted/30 → --muted: #f5f3f0 (admin page background, evolved)
  - bg-background → --background: #faf9f7 (admin header, evolved)
  - bg-card → --card: #ffffff (chart containers, unchanged)
  - var(--primary), var(--success), var(--warning) → chart stroke/fill colors
  - --border: #e8e4df (table borders, evolved)
  - --foreground: #0f172a (body text, unchanged)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build verification</name>
  <files></files>
  <read_first>
    - tests/design-system.test.ts
    - src/app/globals.css
    - package.json
  </read_first>
  <action>
Run the complete verification suite to confirm all Phase 5 automated requirements pass:

1. Run the full build:
```bash
bun run build
```
Build must exit 0 with no errors.

2. Run the design system test suite:
```bash
bunx vitest run tests/design-system.test.ts
```
All tests must pass: dark mode removed (DSYS-01), palette evolution (DSYS-02), animation utilities (DSYS-03), libraries installed (DSYS-04).

3. Run a comprehensive grep to confirm no remaining dark mode or hardcoded color artifacts:
```bash
# No dark mode media query in globals.css
grep -c "prefers-color-scheme: dark" src/app/globals.css
# Should return 0

# No --hero references in any source file
grep -r "var(--hero)" src/
# Should return nothing

# No hardcoded hex colors in page.tsx
grep -c "#c8102e\|#ef4444\|#a80d26" src/app/page.tsx
# Should return 0

# Confirm color-scheme: light is set
grep "color-scheme: light" src/app/globals.css
# Should return the line
```

4. If any check fails, report the exact failure. Do NOT attempt to fix — that is Plan 01's responsibility and it should already be complete.
  </action>
  <verify>
    <automated>cd /Users/richard/Developer/tlc-autos && bun run build 2>&1 | tail -3 && bunx vitest run tests/design-system.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits with code 0
    - `bunx vitest run tests/design-system.test.ts` shows all tests passing (0 failures)
    - `grep -c "prefers-color-scheme: dark" src/app/globals.css` outputs `0`
    - `grep -r "var(--hero)" src/` returns no results
    - `grep "color-scheme: light" src/app/globals.css` returns a matching line
  </acceptance_criteria>
  <done>Full build succeeds, all design system tests pass, no stale dark mode or hardcoded color artifacts remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify admin dashboard and site rendering</name>
  <what-built>
Light-only design system with evolved warm palette (Phase 5, Plan 01). Dark mode removed, palette shifted to warm whites (#faf9f7), burgundy secondary (#a81225), warm borders (#e8e4df). Homepage updated to use semantic CSS classes instead of hardcoded hex colors and --hero variables.
  </what-built>
  <how-to-verify>
**Set your OS to dark mode first** (System Settings > Appearance > Dark), then verify:

**1. Admin Dashboard** (http://localhost:3000/admin)
- [ ] Sidebar renders dark navy — NOT white or transparent
- [ ] Content area has warm white background — NOT dark
- [ ] Dashboard charts (if data exists) are readable — bars/lines visible on white cards
- [ ] Tables have visible borders and readable text
- [ ] Form inputs have visible borders and focus rings (click into a field)

**2. Homepage** (http://localhost:3000)
- [ ] Hero section has light/warm background — NOT dark navy
- [ ] Hero text is readable (dark text on light background)
- [ ] "Browse Inventory" button is burgundy (#a81225), not bright red
- [ ] No gradient accent lines visible
- [ ] Stats bar is readable with proper contrast
- [ ] CTA section at bottom has light background — NOT dark navy
- [ ] CTA buttons use burgundy color

**3. Other Public Pages** (spot-check 2-3)
- [ ] /inventory — renders with warm white palette
- [ ] /contact — renders with warm white palette
- [ ] No page shows dark backgrounds on any OS dark mode setting

**If dev server is not running:** Start with `bun dev` in a separate terminal.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues (e.g., "admin charts are washed out", "hero text invisible")</resume-signal>
</task>

</tasks>

<verification>
1. Build passes: `bun run build` exits 0
2. All design system tests pass: `bunx vitest run tests/design-system.test.ts` — 0 failures
3. Human visual verification of admin dashboard confirms DSYS-05
4. No dark backgrounds appear on any page with OS dark mode enabled
</verification>

<success_criteria>
- Full automated test suite passes (DSYS-01 through DSYS-04 verified)
- Admin dashboard visually confirmed functional (DSYS-05 verified)
- No visual regressions on public pages
- Site renders light-only regardless of OS dark mode setting
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-foundation/05-02-SUMMARY.md`
</output>

---
phase: 03-admin-data-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/layout.tsx
  - src/components/admin/data-table.tsx
  - src/components/admin/data-table-pagination.tsx
  - src/components/admin/data-table-toolbar.tsx
  - src/hooks/use-table-filters.ts
  - src/app/api/admin/vehicles/route.ts
  - src/app/api/admin/customers/route.ts
  - src/app/api/admin/leads/route.ts
  - src/app/api/admin/deals/route.ts
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "A shared DataTable component renders any column definition with sorting, pagination, loading, empty state, and row click"
    - "All four API routes accept sort and order query params and return correctly ordered results"
    - "nuqs hooks work in any admin page (NuqsAdapter is present)"
    - "Filter state (search, status, sort, order, page) is stored in URL query params via nuqs"
  artifacts:
    - path: "src/components/admin/data-table.tsx"
      provides: "Generic DataTable<TData> component"
      exports: ["DataTable"]
      min_lines: 60
    - path: "src/components/admin/data-table-pagination.tsx"
      provides: "Pagination controls with page numbers"
      exports: ["DataTablePagination"]
    - path: "src/components/admin/data-table-toolbar.tsx"
      provides: "Search input + status filter dropdown"
      exports: ["DataTableToolbar"]
    - path: "src/hooks/use-table-filters.ts"
      provides: "nuqs URL state hook for table filters"
      exports: ["useTableFilters"]
    - path: "src/app/admin/layout.tsx"
      provides: "NuqsAdapter wrapper"
      contains: "NuqsAdapter"
  key_links:
    - from: "src/components/admin/data-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable with manualSorting + manualPagination"
      pattern: "manualSorting.*true"
    - from: "src/hooks/use-table-filters.ts"
      to: "nuqs"
      via: "useQueryStates"
      pattern: "useQueryStates"
    - from: "src/app/admin/layout.tsx"
      to: "nuqs/adapters/next/app"
      via: "NuqsAdapter wrapping children"
      pattern: "NuqsAdapter"
---

<objective>
Build the shared DataTable infrastructure and add sort support to all API routes so that entity list pages (Plan 02) can consume them.

Purpose: This is the foundation layer -- DataTable component, URL state hook, API sort params, and NuqsAdapter. Without these, the entity page rewrites cannot function.
Output: Reusable DataTable components, useTableFilters hook, 4 updated API routes, NuqsAdapter in admin layout.
</objective>

<execution_context>
@/Users/richard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-data-dashboard/03-CONTEXT.md
@.planning/phases/03-admin-data-dashboard/03-RESEARCH.md

<interfaces>
<!-- Existing shadcn Table components used by DataTable -->
From src/components/ui/table.tsx:
Table, TableHeader, TableBody, TableRow, TableHead, TableCell (standard shadcn exports)

From src/components/ui/input.tsx:
Input (standard shadcn)

From src/components/ui/select.tsx:
Select, SelectContent, SelectItem, SelectTrigger, SelectValue (standard shadcn)

From src/components/ui/button.tsx:
Button with variant and size props

From src/components/ui/skeleton.tsx:
Skeleton (for loading states)

From src/lib/utils.ts:
export function cn(...inputs: ClassValue[]): string
export function sanitizeSearch(input: string): string

<!-- Existing API route pattern (vehicles as reference) -->
From src/app/api/admin/vehicles/route.ts:
GET handler: requireAuth() -> query with .select(), .eq(), .or(), .order(), .range() -> NextResponse.json({ vehicles, pagination: { page, limit, total, totalPages } })
Currently hardcoded: .order("date_added", { ascending: false })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NuqsAdapter to admin layout + create useTableFilters hook</name>
  <files>src/app/admin/layout.tsx, src/hooks/use-table-filters.ts</files>
  <read_first>
    - src/app/admin/layout.tsx (current admin layout, must preserve sidebar/header/mobile menu)
    - src/app/globals.css (design tokens for reference)
  </read_first>
  <action>
1. **src/app/admin/layout.tsx** -- Add NuqsAdapter wrapper:
   - Add import: `import { NuqsAdapter } from "nuqs/adapters/next/app";`
   - Wrap the `<main>` children (NOT the entire layout) with `<NuqsAdapter>`:
     ```
     <main className="flex-1 overflow-y-auto p-4 lg:p-6">
       <NuqsAdapter>{children}</NuqsAdapter>
     </main>
     ```
   - Preserve ALL existing layout code: sidebar, header, mobile overlay, sign-out, NAV_ITEMS unchanged.

2. **src/hooks/use-table-filters.ts** -- Create URL state hook:
   - Import: `useQueryStates`, `parseAsString`, `parseAsInteger` from `"nuqs"`
   - Export `useTableFilters` function accepting optional `{ defaultSort?: string; defaultOrder?: string; statusOptions?: string[] }` config
   - Return `useQueryStates` result with these params:
     - `search`: `parseAsString.withDefault("")`
     - `status`: `parseAsString.withDefault("")`
     - `sort`: `parseAsString.withDefault(config?.defaultSort ?? "created_at")`
     - `order`: `parseAsString.withDefault(config?.defaultOrder ?? "desc")`
     - `page`: `parseAsInteger.withDefault(1)`
   - Pass options: `{ throttleMs: 300 }`
   - Also export a `buildApiParams` helper function that takes the filters object and returns a `URLSearchParams`:
     ```typescript
     export function buildApiParams(filters: { search: string; status: string; sort: string; order: string; page: number }, limit = 20): URLSearchParams {
       const params = new URLSearchParams();
       if (filters.search) params.set("search", filters.search);
       if (filters.status) params.set("status", filters.status);
       params.set("sort", filters.sort);
       params.set("order", filters.order);
       params.set("page", String(filters.page));
       params.set("limit", String(limit));
       return params;
     }
     ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/layout.tsx contains `import { NuqsAdapter } from "nuqs/adapters/next/app"`
    - src/app/admin/layout.tsx contains `<NuqsAdapter>`
    - src/hooks/use-table-filters.ts contains `export function useTableFilters`
    - src/hooks/use-table-filters.ts contains `useQueryStates`
    - src/hooks/use-table-filters.ts contains `throttleMs: 300`
    - src/hooks/use-table-filters.ts contains `export function buildApiParams`
    - src/hooks/use-table-filters.ts contains `parseAsInteger.withDefault(1)`
  </acceptance_criteria>
  <done>NuqsAdapter wraps admin children, useTableFilters hook exports URL state management with 300ms throttle and buildApiParams helper</done>
</task>

<task type="auto">
  <name>Task 2: Create DataTable component, pagination, toolbar, and add sort params to all 4 API routes</name>
  <files>
    src/components/admin/data-table.tsx,
    src/components/admin/data-table-pagination.tsx,
    src/components/admin/data-table-toolbar.tsx,
    src/app/api/admin/vehicles/route.ts,
    src/app/api/admin/customers/route.ts,
    src/app/api/admin/leads/route.ts,
    src/app/api/admin/deals/route.ts
  </files>
  <read_first>
    - src/app/api/admin/vehicles/route.ts (current hardcoded .order pattern)
    - src/app/api/admin/customers/route.ts (current GET handler)
    - src/app/api/admin/leads/route.ts (current GET handler)
    - src/app/api/admin/deals/route.ts (current GET handler)
    - src/components/ui/table.tsx (shadcn Table exports)
    - src/components/ui/skeleton.tsx (for loading states)
    - src/components/ui/select.tsx (for status filter)
    - src/components/ui/input.tsx (for search)
    - src/components/ui/button.tsx (for pagination buttons)
    - src/lib/utils.ts (cn function, sanitizeSearch)
  </read_first>
  <action>
1. **src/components/admin/data-table.tsx** -- Generic DataTable component:
   - "use client" directive
   - Import from `@tanstack/react-table`: `useReactTable`, `getCoreRowModel`, `flexRender`, `type ColumnDef`, `type SortingState`, `type PaginationState`, `type OnChangeFn`
   - Import shadcn Table/TableHeader/TableBody/TableRow/TableHead/TableCell
   - Import Skeleton from `@/components/ui/skeleton`
   - Import `ArrowUpDown`, `ArrowUp`, `ArrowDown` from `lucide-react`
   - Define generic interface:
     ```typescript
     interface DataTableProps<TData> {
       columns: ColumnDef<TData, unknown>[];
       data: TData[];
       pageCount: number;
       sorting: SortingState;
       onSortingChange: OnChangeFn<SortingState>;
       pagination: PaginationState;
       onPaginationChange: OnChangeFn<PaginationState>;
       isLoading?: boolean;
       onRowClick?: (row: TData) => void;
     }
     ```
   - Create `DataTable<TData>` function component:
     - Call `useReactTable` with: `data`, `columns`, `pageCount`, `state: { sorting, pagination }`, `onSortingChange`, `onPaginationChange`, `manualSorting: true`, `manualPagination: true`, `getCoreRowModel: getCoreRowModel()`
     - If `isLoading`, render 5 Skeleton rows (TableRow with TableCell containing Skeleton elements, colSpan matched to column count)
     - If not loading and no data, render empty state row: "No results found." centered in muted-foreground
     - Otherwise render data rows using `flexRender`
     - For each header, render sort indicator: `ArrowUp` if asc, `ArrowDown` if desc, `ArrowUpDown` if unsorted. Only show on columns with `enableSorting !== false`.
     - When `onRowClick` provided, add `cursor-pointer hover:bg-muted/50` to data rows and call `onRowClick(row.original)` on click
   - Export `DataTable` and re-export `type ColumnDef` for convenience

2. **src/components/admin/data-table-pagination.tsx** -- Pagination controls:
   - "use client" directive
   - Import Button from `@/components/ui/button`
   - Import `ChevronLeft`, `ChevronRight`, `ChevronsLeft`, `ChevronsRight` from `lucide-react`
   - Props: `pageIndex: number`, `pageCount: number`, `onPageChange: (page: number) => void`, `totalItems: number`
   - Render: "Showing page {pageIndex + 1} of {pageCount} ({totalItems} total)" on left
   - Render on right: First (ChevronsLeft), Previous (ChevronLeft), page number buttons (show max 5: current +/- 2), Next (ChevronRight), Last (ChevronsRight)
   - Disable First/Previous when pageIndex === 0, disable Next/Last when pageIndex >= pageCount - 1
   - Export `DataTablePagination`

3. **src/components/admin/data-table-toolbar.tsx** -- Search + status filter:
   - "use client" directive
   - Import Input, Select/SelectContent/SelectItem/SelectTrigger/SelectValue, Button
   - Import `Search`, `X` from `lucide-react`
   - Props: `search: string`, `onSearchChange: (value: string) => void`, `statusOptions?: { label: string; value: string }[]`, `status?: string`, `onStatusChange?: (value: string) => void`, `onClear?: () => void`
   - Render search Input with Search icon (same pattern as existing vehicle page: `pl-9` with absolute icon)
   - If statusOptions provided, render Select dropdown with "All" default option (value "") + status options
   - If search or status has value, show X clear button calling onClear
   - Export `DataTableToolbar`

4. **API Route Updates** -- Add sort/order params to all 4 GET handlers:
   For each route, add BEFORE the existing `.order()` call:
   ```typescript
   const sortBy = searchParams.get("sort") || "{DEFAULT_COLUMN}";
   const sortOrder = searchParams.get("order") || "desc";
   const ALLOWED_SORT = ["{list of columns}"];
   const validSort = ALLOWED_SORT.includes(sortBy) ? sortBy : "{DEFAULT_COLUMN}";
   ```
   Then replace the hardcoded `.order()` with:
   ```typescript
   .order(validSort, { ascending: sortOrder === "asc" })
   ```

   Per-route allowed columns and defaults:
   - **vehicles/route.ts**: DEFAULT=`"date_added"`, ALLOWED=`["date_added", "year", "make", "model", "selling_price", "status", "stock_number", "mileage"]`
   - **customers/route.ts**: DEFAULT=`"created_at"`, ALLOWED=`["created_at", "first_name", "last_name", "email", "phone"]`
   - **leads/route.ts**: DEFAULT=`"created_at"`, ALLOWED=`["created_at", "first_name", "last_name", "status", "source"]`
   - **deals/route.ts**: DEFAULT=`"sale_date"`, ALLOWED=`["sale_date", "status", "total_price", "sale_type", "deal_number"]`

   IMPORTANT: Only modify the GET handler in each file. Do NOT touch the POST handler.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/data-table.tsx contains `manualSorting: true`
    - src/components/admin/data-table.tsx contains `manualPagination: true`
    - src/components/admin/data-table.tsx contains `getCoreRowModel`
    - src/components/admin/data-table.tsx contains `export function DataTable`
    - src/components/admin/data-table.tsx contains `isLoading`
    - src/components/admin/data-table.tsx contains `Skeleton`
    - src/components/admin/data-table-pagination.tsx contains `export function DataTablePagination`
    - src/components/admin/data-table-pagination.tsx contains `pageCount`
    - src/components/admin/data-table-toolbar.tsx contains `export function DataTableToolbar`
    - src/components/admin/data-table-toolbar.tsx contains `statusOptions`
    - src/app/api/admin/vehicles/route.ts contains `ALLOWED_SORT`
    - src/app/api/admin/vehicles/route.ts contains `sortOrder === "asc"`
    - src/app/api/admin/customers/route.ts contains `ALLOWED_SORT`
    - src/app/api/admin/leads/route.ts contains `ALLOWED_SORT`
    - src/app/api/admin/deals/route.ts contains `ALLOWED_SORT`
  </acceptance_criteria>
  <done>DataTable renders columns with sort indicators and loading skeletons, DataTablePagination shows page controls, DataTableToolbar has search+filter, all 4 API routes accept sort/order params with column allowlist validation</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run` passes (existing tests unbroken)
- All 4 API routes have ALLOWED_SORT arrays preventing sort injection
- DataTable component exists with manualSorting + manualPagination
- NuqsAdapter wraps admin layout children
</verification>

<success_criteria>
- DataTable, DataTablePagination, DataTableToolbar components export from src/components/admin/
- useTableFilters hook exports from src/hooks/
- NuqsAdapter wraps admin children in layout.tsx
- All 4 API GET routes support sort and order query params with allowlist validation
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-data-dashboard/03-01-SUMMARY.md`
</output>

pre-commit:
  parallel: false
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: npx eslint {staged_files}
      stage_fixed: true

    typecheck:
      run: npx tsc --noEmit

    build:
      run: npm run build
